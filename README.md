# mini-agentic-rag-system
A mini agentic RAG system built with Python and LangGraph. It ingests a small text/markdown knowledge base, builds a graph-style retrieval layer, and exposes a chat agent with custom tools, reasoning, memory, error handling, and multi-step graph execution.
